# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
.eggs/
venv/
.venv/
env/

# Node
node_modules/
frontend/node_modules/
frontend/dist/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
# NOTE: .env.production is NOT ignored — it must be committed for the Docker
# build to pick up the correct VITE_API_URL for HF Spaces / same-origin deploy.

# Reports
*.pdf
!README.pdf

# Reference docs (not needed in repo)
ProctorVision_Reference.txt
ProctorVision_Canonical_Reference.pdf

# YOLOv8 model cache & large binary ML models — HF Spaces blocks these
# on push. They download at runtime on first use.
*.pt
backend/yolov8n.pt
backend/models/face_landmarker.task
runs/

# Local SQLite DB (dev only; production lives on Render's persistent disk)
backend/data/
*.db
*.db-journal
